/**
 * Marcas: as rotas administrativas compartilham o mesmo modulo de categorias
 * (ambas sao catalogo simples). Este arquivo existe para manter a organizacao
 * por dominio na pasta `admin/`.
 */
export { brandAdminRoutes } from "./category.admin.routes.js";
